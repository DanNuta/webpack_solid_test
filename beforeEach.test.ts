beforeEach(() => {
  const items = ["elephant", "giraffe", "lion"];
});

// test("should add a new element to begining of the array", () => {
//   const items = ["elephant", "giraffe", "lion"];

//   items.unshift("apple");

//   expect(items[0]).toBe("apple");
// });

// test("chech the length of the array", () => {
//   expect(items.length).toBe(items.length);
// });

test.only("my test", () => {
  expect(typeof "stritn").toBe("string");
});
